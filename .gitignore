# Foundry
/out
/cache
/broadcast
/zkout
/docs

# Local economy-tuning models (python; the Forge sims in test/simulation/*.t.sol stay tracked)
/test/simulation/*.py
/test/simulation/__pycache__/

# Trait content: large (~2MB) and reproducible from the trait generator
/script/data/traits.json
# Gzipped JS payload: built from the frontend, reproducible
/script/data/dealers.js.gz.b64
# assignments.json
/script/data/assignments.json
# Local debug artifacts
/script/data/debug/
/script/debug/
/script/*.md
# Everything else under /script/data is critical state we DO track:
#   - deployments/{network}.json (contract addresses per network)
#   - {network}/pointers.json    (FileStore pointers per network)

# Dependencies
/lib

# Environment
.env

# Deployment state
script/deployed-addresses.env

# OS
.DS_Store
